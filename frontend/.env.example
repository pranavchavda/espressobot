# Add this to your .env file to configure Google Workspace integration

# Option 1: Use direct Google API integration (single sign-in)
GOOGLE_WORKSPACE_MODE=direct

# Option 2: Use MCP server (requires separate authentication)
# GOOGLE_WORKSPACE_MODE=mcp

# GA4 Analytics Integration
# To enable GA4 analytics:
# 1. Set ga4_enabled=true in your user record
# 2. Set ga4_property_id to your GA4 property ID (e.g., "123456789")
# 3. Ensure Google Analytics readonly scope is authorized
# 4. Link Google Ads account to GA4 for advertising metrics