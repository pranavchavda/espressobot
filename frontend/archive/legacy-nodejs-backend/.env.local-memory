# EspressoBot Local Memory Configuration
# Use this for local-only memory storage (no API limits)

# Force local memory usage (even if MEM0_API_KEY is set)
MEM0_USE_LOCAL=true

# OpenAI API key (required for embeddings and LLM operations)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Remove or comment out MEM0_API_KEY to avoid accidental API usage
# MEM0_API_KEY=

# Other existing environment variables...